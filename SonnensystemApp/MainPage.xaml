<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="SonnensystemApp.MainPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    BackgroundColor="#050816">

    <Grid RowDefinitions="Auto,*">

        <!-- Top panel -->
        <Border
            Grid.Row="0"
            BackgroundColor="transparent"
            StrokeThickness="0"
            Padding="16,14,16,12">

            <Grid ColumnDefinitions="*,Auto"
                  ColumnSpacing="24">

                <!-- Left side -->
                <VerticalStackLayout Grid.Column="0"
                                     Spacing="2"
                                     VerticalOptions="Center">
                    <Label Text="Sonnensystem App"
                           FontSize="28"
                           FontAttributes="Bold"
                           TextColor="White" />

                    <Label Text="Echtzeit-Visualisierung der Planeten"
                           FontSize="14"
                           TextColor="#B8C0D9" />
                </VerticalStackLayout>

                <!-- Right side -->
                <HorizontalStackLayout Grid.Column="1"
                                       Spacing="8"
                                       HorizontalOptions="End"
                                       VerticalOptions="Center">

                    <Button Text="1x"
                            Clicked="Speed1x"
                            BackgroundColor="#4F46E5"
                            TextColor="White" />

                    <Button Text="500K x"
                            Clicked="Speed500Kx"
                            BackgroundColor="#4F46E5"
                            TextColor="White" />

                    <Button Text="1М x"
                            Clicked="Speed1Мx"
                            BackgroundColor="#4F46E5"
                            TextColor="White" />

                    <Button Text="10M x"
                            Clicked="SpeedMega"
                            BackgroundColor="#4F46E5"
                            TextColor="White" />

                    <Button Text="🛸 Sind wir allein im Universum?"
                            Clicked="OnAlienQuestionClicked"
                            HorizontalOptions="End"
                            VerticalOptions="End"
                            Margin="0,0,24,24"
                            Padding="18,10"
                            BackgroundColor="#AA4F46E5"
                            TextColor="White"
                            CornerRadius="12"
                            FontSize="14"
                            ZIndex="10" />
                </HorizontalStackLayout>

            </Grid>
        </Border>

        <!-- Canvas -->
        <GraphicsView
            x:Name="SolarSystemCanvas"
            Grid.Row="1"
            HorizontalOptions="Fill"
            VerticalOptions="Fill" />
    </Grid>
</ContentPage>